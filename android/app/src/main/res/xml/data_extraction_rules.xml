<?xml version="1.0" encoding="utf-8"?>
<!--
  Deen Daily keeps everything on the device: streaks, checklists, journal
  entries and the stored coordinates live in the WebView's local storage.
  Cloud backup and device-to-device transfer would copy them off the phone,
  so both are refused outright.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
